# dependencias
node_modules/

# sessao do WhatsApp (Baileys) — NUNCA versionar
auth/

# credenciais e segredos
credentials.json
.env

# banco de reservas (dados locais/producao) — nao versionar
reservas.db
reservas.db-shm
reservas.db-wal

# sistema
.DS_Store
